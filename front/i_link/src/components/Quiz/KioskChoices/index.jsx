import React from "react";
import { Box } from "@mui/material";

const QuizKioskChoices = ({ quiz }) => {
  const { sel1, sel2, sel3, sel4, sel1Url, sel2Url, sel3Url, sel4Url } = quiz;

  return <Box></Box>;
};

export default QuizKioskChoices;
